% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{cindex_weights}
\alias{cindex_weights}
\alias{weights_harrell}
\alias{weights_uno}
\alias{weights_truncated}
\alias{weights_custom}
\title{Pair weightings for concordance decomposition}
\usage{
weights_harrell()

weights_uno(tau = NULL)

weights_truncated(tau)

weights_custom(fn, name = "Custom")
}
\arguments{
\item{tau}{Truncation time. For \code{weights_uno()}, pairs whose earlier
event time exceeds \code{tau} receive weight zero; \code{NULL} (default) means
no truncation. For \code{weights_truncated()}, \code{tau} is required.}

\item{fn}{A function of two arguments \verb{(t, G)} returning a single
non-negative finite number. \code{t} is the event time of the earlier
member of the pair; \code{G} is a function returning the Kaplan-Meier
censoring survival probability at a given time.}

\item{name}{A length-one character label used in printed output and
plot annotations.}
}
\value{
An object of class \code{cindex_weights}.
}
\description{
A concordance index of the form
\code{C = sum(w_ij * c_ij) / sum(w_ij)} is fully determined by its pair
weighting \code{w_ij}. These constructors supply that weighting to
\code{\link[=decompose_cindex]{decompose_cindex()}}. \code{weights_harrell()} gives Harrell's C,
\code{weights_uno()} gives Uno's inverse-probability-of-censoring-weighted
C, and \code{weights_custom()} accepts any user-supplied rule.
}
\examples{
weights_harrell()
weights_uno()
weights_truncated(tau = 365)
weights_custom(function(t, G) 1 / G(t), name = "IPCW (power 1)")

}
