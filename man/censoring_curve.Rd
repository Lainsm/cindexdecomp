% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/censoring_curve.R
\name{censoring_curve}
\alias{censoring_curve}
\alias{print.cindex_curve}
\alias{as.data.frame.cindex_curve}
\title{Trace the decomposition as administrative censoring increases}
\usage{
censoring_curve(
  time,
  status,
  risk,
  weights = weights_harrell(),
  higher_is_riskier = TRUE,
  n_thresholds = 20,
  probs = seq(0.05, 0.7, length.out = n_thresholds),
  min_pairs = NULL
)

\method{print}{cindex_curve}(x, ...)

\method{as.data.frame}{cindex_curve}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{time, status, risk}{Numeric vectors of equal length.}

\item{weights}{A \code{cindex_weights} object. Defaults to
\code{\link[=weights_harrell]{weights_harrell()}}.}

\item{higher_is_riskier}{Logical; see \code{\link[=decompose_cindex]{decompose_cindex()}}.}

\item{n_thresholds}{Number of cut-offs to evaluate. Ignored if \code{probs}
is supplied.}

\item{probs}{Quantiles of the observed event times at which to place the
cut-offs.}

\item{min_pairs}{Minimum number of event-event pairs for an estimate to
be considered precise. Rows below it are \strong{flagged} in the
\code{low_precision} column, never removed and never set to \code{NA}. \code{NULL}
(default) selects \code{max(50, round(n_events^1.5 * 0.1))}.}

\item{x}{A \code{cindex_curve} object.}

\item{...}{Ignored.}

\item{row.names}{Ignored.}

\item{optional}{Ignored.}
}
\value{
An object of class \code{cindex_curve}. Its \verb{$data} element has one
row per threshold, with columns \code{threshold}, \code{censoring}, \code{C_ee},
\code{C_ec}, \code{C_global}, \code{gap}, \code{N_ee}, \code{N_ec}, \code{low_precision}, \code{W_ee} and
\code{W_ec}. \code{W_ee}/\code{W_ec} are the weighted totals behind \code{C_ee}/\code{C_ec}
(\code{C_ee = S_ee / W_ee}, etc.), so the decomposition identity is
verifiable at every row:
\code{C_global == (W_ee * C_ee + W_ec * C_ec) / (W_ee + W_ec)}. \code{N_ee}/
\code{N_ec} are pair \emph{counts} and only equal \code{W_ee}/\code{W_ec} under a weighting
that assigns every comparable pair weight 1 (Harrell's); under
\code{\link[=weights_uno]{weights_uno()}} or another non-unit weighting the identity does not
reconstruct from counts alone.
}
\description{
Sweeps a series of administrative cut-off times over the cohort. At each
cut-off, subjects whose event falls after the cut-off are treated as
censored there, and the decomposition is recomputed. This traces how the
global C-index behaves as censoring rises, while \code{C_ee} -- the harder
ranking task -- is tracked separately.

The cut-off is applied to the \strong{whole cohort}, so the reported
censoring rate is the cohort's actual censoring rate at that cut-off.
}
\examples{
set.seed(42)
time   <- rexp(300, rate = 0.1)
status <- rbinom(300, 1, 0.6)
risk   <- rnorm(300)
censoring_curve(time, status, risk, n_thresholds = 8)

}
