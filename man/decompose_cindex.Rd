% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose.R, R/methods.R
\name{decompose_cindex}
\alias{decompose_cindex}
\alias{decompose_cindex.default}
\alias{decompose_cindex.formula}
\alias{print.cindex_decomp}
\alias{summary.cindex_decomp}
\alias{as.data.frame.cindex_decomp}
\title{Decompose a concordance index into Event-Event and Event-Censored parts}
\usage{
decompose_cindex(x, ...)

\method{decompose_cindex}{default}(
  x,
  status,
  risk,
  weights = weights_harrell(),
  higher_is_riskier = TRUE,
  n_boot = 0,
  conf_level = 0.95,
  ...
)

\method{decompose_cindex}{formula}(
  x,
  data = parent.frame(),
  weights = weights_harrell(),
  higher_is_riskier = TRUE,
  n_boot = 0,
  conf_level = 0.95,
  ...
)

\method{print}{cindex_decomp}(x, ...)

\method{summary}{cindex_decomp}(object, ...)

\method{as.data.frame}{cindex_decomp}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{A \code{cindex_decomp} object.}

\item{...}{Ignored.}

\item{status}{Numeric vector of event indicators (1 = event,
0 = censored). Default method only.}

\item{risk}{Numeric vector of risk scores. Default method only.}

\item{weights}{A \code{cindex_weights} object. Defaults to
\code{\link[=weights_harrell]{weights_harrell()}}.}

\item{higher_is_riskier}{Logical. \code{TRUE} (default) if larger values of
\code{risk} indicate higher hazard. If \code{FALSE}, \code{risk} is negated once
before any pair counting, so every component stays mutually
consistent. Values below 0.5 are reported as they are: a
worse-than-chance model is a finding, not an error.}

\item{n_boot}{Number of bootstrap replicates. \code{0} (default) skips
resampling, in which case \code{\link[=confint]{confint()}} is unavailable. Replicates
resample subjects, not pairs.}

\item{conf_level}{Confidence level stored on the object and used as the
default for \code{\link[=confint]{confint()}}.}

\item{data}{A data frame in which to evaluate the formula. Formula
method only.}

\item{object}{A \code{cindex_decomp} object.}

\item{row.names}{Ignored.}

\item{optional}{Ignored.}
}
\value{
An object of class \code{cindex_decomp}.
}
\description{
Splits a concordance index into the two ranking tasks it pools: pairs
where both subjects had the event (\code{C_ee}), and pairs where one had the
event and one was censored (\code{C_ec}). As censoring rises the event-event
pairs vanish and \code{C_ec} dominates the global score, so a model close to
chance on true events can still report a stable global C-index.

The decomposition applies to any concordance index of the form
\code{C = sum(w_ij * c_ij) / sum(w_ij)}. The weighting is chosen with
\code{\link[=weights_harrell]{weights_harrell()}}, \code{\link[=weights_uno]{weights_uno()}}, \code{\link[=weights_truncated]{weights_truncated()}} or
\code{\link[=weights_custom]{weights_custom()}}, and the identity
\code{C = (W_ee * C_ee + W_ec * C_ec) / (W_ee + W_ec)} holds for each.
}
\examples{
set.seed(42)
time   <- rexp(200, rate = 0.1)
status <- rbinom(200, 1, 0.6)
risk   <- rnorm(200)

decompose_cindex(time, status, risk)
decompose_cindex(time, status, risk, weights = weights_uno())

}
